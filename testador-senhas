<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Segurança de Senhas</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #0c0c0c;
      color: #f0f0f0;
      margin: 0;
      padding: 20px;
    }

    header, main, footer {
      max-width: 600px;
      margin: auto;
    }

    header h1 {
      text-align: center;
      color: #00f7ff;
    }

    .dicas {
      background-color: #1c1c1c;
      padding: 15px;
      border-left: 4px solid #00f7ff;
      margin-bottom: 20px;
    }

    .verificador {
      background-color: #1c1c1c;
      padding: 20px;
      border-radius: 8px;
    }

    input[type="password"] {
      width: 100%;
      padding: 10px;
      margin-top: 10px;
      font-size: 16px;
    }

    #forca {
      margin-top: 10px;
      font-weight: bold;
    }

    footer {
      text-align: center;
      margin-top: 30px;
      font-size: 12px;
      color: #888;
    }
  </style>
</head>
<body>

  <header>
    <h1>🔒 Segurança de Senhas</h1>
  </header>

  <main>
    <section class="dicas">
      <h2>Dicas para uma senha segura:</h2>
      <ul>
        <li>Use pelo menos 8 caracteres</li>
        <li>Inclua letras maiúsculas, minúsculas, números e símbolos</li>
        <li>Evite usar dados pessoais ou palavras comuns</li>
        <li>Não reutilize senhas em diferentes sites</li>
      </ul>
    </section>

    <section class="verificador">
      <h2>Teste a força da sua senha:</h2>
      <input type="password" id="senha" placeholder="Digite sua senha..." oninput="verificarSenha()">
      <p id="forca">Força: -</p>
    </section>
  </main>

  <footer>
    <p>Projeto educativo de segurança da informação | &copy; 2025</p>
  </footer>

  <script>
    function verificarSenha() {
      const senha = document.getElementById("senha").value;
      const forca = document.getElementById("forca");

      let pontuacao = 0;
      if (senha.length >= 8) pontuacao++;
      if (/[A-Z]/.test(senha)) pontuacao++;
      if (/[a-z]/.test(senha)) pontuacao++;
      if (/[0-9]/.test(senha)) pontuacao++;
      if (/[^A-Za-z0-9]/.test(senha)) pontuacao++;

      let nivel = "";
      switch (pontuacao) {
        case 0:
        case 1:
          nivel = "Muito Fraca 🔴";
          break;
        case 2:
          nivel = "Fraca 🟠";
          break;
        case 3:
          nivel = "Média 🟡";
          break;
        case 4:
          nivel = "Forte 🟢";
          break;
        case 5:
          nivel = "Muito Forte 🟢🔒";
          break;
      }

      forca.textContent = `Força: ${nivel}`;
    }
  </script>

</body>
</html>
